name: UTILIA Final Formatter

on:
  push:
    branches: [master]

permissions:
  contents: write

jobs:
  format:
    if: ${{ !contains(github.event.head_commit.message, '[utilia-format-final]') }}
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v6
      - name: Set up Flutter
        uses: subosito/flutter-action@v2
        with:
          channel: stable
          flutter-version: '3.35.7'
          cache: false
          pub-cache: false
      - name: Format and commit
        shell: bash
        run: |
          set -euo pipefail
          dart format lib test
          rm .github/workflows/oneoff-format-final.yml
          git config user.name 'UTILIA Release Bot'
          git config user.email 'actions@users.noreply.github.com'
          git add lib test .github/workflows/oneoff-format-final.yml
          git commit -m 'style: finalize Dart formatting [utilia-format-final]'
          git push
